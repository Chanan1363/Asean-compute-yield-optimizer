# ASEAN Grid Prototype — dependencies (ทั้งหมด OPTIONAL)
# แกนระบบ (core/) รันด้วย Python stdlib ล้วน — ไม่ต้องติดตั้งอะไร
#
# สำหรับ API / dev server:
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
#
# สำหรับ sandbox จริง (แทน stub):
# docker>=7.0
#
# สำหรับ AI fine-tuning (แทน trainer stub):
# scikit-learn>=1.4     # จุดเริ่มต้น: gradient boosting
# torch / unsloth / axolotl  # เมื่อต้องการโมเดลใหญ่

#
# สำหรับ Postgres (Supabase/Neon — ข้อมูลถาวร):
pg8000>=1.31
certifi>=2024.6
